# AGENTS.md

## Cursor Cloud specific instructions

### Repository Overview

This is **[Unknown Project]** — an empty repository ready for brainstorming and development. No source code, project files, or dependencies exist yet.

### Development Environment

- **No package managers or dependency files** are present (no `package.json`, `requirements.txt`, `Cargo.toml`, etc.).
- **No build system** is configured (no Makefile, CMake, SCons, etc.).
- **No Docker services** are used.
- **No linting or testing frameworks** are configured yet.

### Notes for Future Agents

- The environment is a blank slate. Once project files and source code are added, revisit this file to document the chosen tech stack, dependencies, and setup instructions.
- Refer to the git history (`git show a19c972:.cursorrules`) for previously considered GDScript/Godot coding conventions.
